//! CRDT-based sync via Yrs (Yjs-compatible).
//!
//! Re-exports from `yrs_sync` where the `SyncDocument` and
//! `merge_update` / `snapshot` functions are implemented.

pub use crate::sync::yrs_sync::*;
